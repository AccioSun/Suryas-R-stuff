Patient1 <- c("0.6", "103", "bad", "low", "low")
Patient2 <- c("0.3", "87", "bad", "low", "high")
Patient3 <- c("0.4", "32", "bad", "high", "low")
Patient4 <- c("0.4", "42", "bad", "high", "high")
Patient5 <- c("0.2", "59", "good", "low", "low")
Patient6 <- c("0.6", "109", "good", "low", "high")
Patient7 <- c("0.3", "78", "good", "high", "low")
Patient8 <- c("0.4", "205", "good", "high", "high")
Patient9 <- c("0.9", "135", "NA", "high", "high")
Patient10 <- c("0.2", "176", "bad", "high", "high")
HospitalRecords <- cbind(Patient1, Patient2, Patient3, Patient4, Patient5, Patient6, Patient7, Patient8, Patient9, Patient10)
Hospital.t <- t(HospitalRecords)
Hospital.t.df <- data.frame(Hospital.t, stringsAsFactors = FALSE)
#At this point, I realized that it would be easier to copy the data over directly
Freq <- c(0.6, 0.3, 0.4, 0.4, 0.2, 0.6, 0.3, 0.4, 0.9, 0.2)
bloodp <- c(103, 87, 32, 42, 59, 109, 78, 205, 135, 176)
first <- c(1, 1, 1, 1, 0, 0, 0, 0, NA, 1)
second <- c(0, 0, 1, 1, 0, 0, 1, 1, 1, 1)
finaldecision <- c(0, 1, 0, 1, 0, 1, 0, 1, 1, 1)
#I focused on the Blood Pressure and Frequency datasets for the box plot because they had the most variation in data.
boxplot(bloodp, horizontal = TRUE, main = "Blood Pressures among Patients", xlab = "Blood Pressure") 
boxplot(Freq, horizontal = TRUE, main = "Frequency of Visits", xlab = "Frequency") 
hist(bloodp)
hist(finaldecision)
